{% set obj = form.vars.value %} {# récupère l'objet envoyé comme paramètre dans la méthode createForm #}

<div class="formulaire">

    <h1 class="bg-secondary mb-4">{% block titre_formulaire %}{% endblock %}</h1>

    {% block formulaire %}{% endblock %}
</div>
