{% extends 'base_admin.html.twig' %}

{% block title %}Modifer Emprunt{% endblock %}

{% block body %}

    {% block formulaire %}
        {{ include('emprunt/_form.html.twig', {'button_label': 'Update', "titre_formulaire": "Modifier l'emprunt n°" ~ emprunt.id}) }}
    {% endblock %}

    <div class="d-flex justify-content-between bg-secondary mt-5">
        <a href="{{ path('emprunt_index') }}" class="btn text-primary">
            <i class="fa fa-reply"></i>
            Retour
        </a>

        {% include 'emprunt/_delete_form.html.twig' with { "emprunt": form.vars.value, "texte": true } %}
    </div>

{% endblock %}
