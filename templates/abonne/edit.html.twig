{% extends 'base.html.twig' %}

{% block title %}Modifer Abonné{% endblock %}

{% block body %}
    <h1>Modifier l'abonné n°{{ abonne.id }}</h1>

    {{ include('abonne/_form.html.twig', {'button_label': 'Actualiser'}) }}

    <div class="d-flex justify-content-between">
        <a href="{{ path('abonne_index') }}">
            <i class="fa fa-reply"></i> Retour à la liste
        </a>

        {{ include('abonne/_delete_form.html.twig') }}
    </div>
{% endblock %}
